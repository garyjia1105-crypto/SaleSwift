PORT=4000
JWT_SECRET=your-super-secret-jwt-key-change-in-production
GEMINI_API_KEY=your-gemini-api-key

# Google OAuth（与前端 VITE_GOOGLE_CLIENT_ID 一致，用于校验 ID Token）
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-web-client-id.apps.googleusercontent.com

# Firebase (Firestore) - 任选其一方式配置：
# 方式一：服务账号 JSON 文件路径（本地开发推荐）
GOOGLE_APPLICATION_CREDENTIALS=./path-to-your-service-account.json

# 方式二：服务账号 JSON 字符串（适合 Vercel/云函数等无文件环境）
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

# 方式三：单独字段
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
